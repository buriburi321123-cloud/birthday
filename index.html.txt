<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Queen's Day</title>

<style>
  body {
    margin: 0;
    height: 100vh;
    background: linear-gradient(270deg, #5f0a87, #a4508b, #ff9a9e);
    background-size: 600% 600%;
    animation: bgMove 15s ease infinite;
    display: flex;
    justify-content: center;
    align-items: center;
    font-family: 'Arial', sans-serif;
    color: white;
    overflow: hidden;
    text-align: center;
  }

  @keyframes bgMove {
    0% { background-position: 0% 50%; }
    50% { background-position: 100% 50%; }
    100% { background-position: 0% 50%; }
  }

  h1 {
    font-size: 64px;
    letter-spacing: 4px;
    animation: fadeIn 2s ease;
  }

  #countdown {
    font-size: 28px;
    margin-top: 20px;
    animation: pulse 2s infinite;
  }

  @keyframes pulse {
    0% { opacity: 1; }
    50% { opacity: 0.7; }
    100% { opacity: 1; }
  }

  @keyframes fadeIn {
    from { opacity: 0; transform: scale(0.9); }
    to { opacity: 1; transform: scale(1); }
  }

  .bottom-text {
    position: absolute;
    bottom: 30px;
    width: 100%;
    font-size: 22px;
    font-weight: bold;
    letter-spacing: 2px;
  }

  /* Teddy bears */
  .teddy {
    position: absolute;
    font-size: 40px;
    animation: float 10s linear infinite;
    opacity: 0.8;
  }

  @keyframes float {
    from { transform: translateY(100vh); }
    to { transform: translateY(-120vh); }
  }
</style>
</head>

<body>

<div>
  <h1 id="title">QUEEN’S DAY</h1>
  <div id="countdown">Loading countdown...</div>
</div>

<div class="bottom-text">
  I LOVE FOREVER AND EVER AND EVER BETU
</div>

<!-- Teddy Bears -->
<div class="teddy" style="left:10%; animation-duration:12s;">🧸</div>
<div class="teddy" style="left:30%; animation-duration:9s;">🧸</div>
<div class="teddy" style="left:50%; animation-duration:11s;">🧸</div>
<div class="teddy" style="left:70%; animation-duration:8s;">🧸</div>
<div class="teddy" style="left:90%; animation-duration:10s;">🧸</div>

<script>
  const targetDate = new Date("December 28, 2025 00:00:00").getTime();

  function updateCountdown() {
    const now = new Date().getTime();
    const distance = targetDate - now;

    if (distance <= 0) {
      document.getElementById("title").innerText = "👑 IT’S QUEEN’S DAY 👑";
      document.getElementById("countdown").innerText =
        "Forever starts now 💖";
      return;
    }

    const days = Math.floor(distance / (1000 * 60 * 60 * 24));
    const hours = Math.floor((distance % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
    const minutes = Math.floor((distance % (1000 * 60 * 60)) / (1000 * 60));
    const seconds = Math.floor((distance % (1000 * 60)) / 1000);

    document.getElementById("countdown").innerText =
      `${days} Days ${hours} Hours ${minutes} Minutes ${seconds} Seconds`;
  }

  setInterval(updateCountdown, 1000);
  updateCountdown();
</script>

</body>
</html>
